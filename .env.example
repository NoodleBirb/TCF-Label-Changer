# HubSpot private app access token
# Create one at: https://app.hubspot.com/l/settings/private-apps
HUBSPOT_ACCESS_TOKEN=

# Server port (client proxies /api here in dev; production serves UI from this port)
PORT=3001

# Set to production when running the built app (npm run build && npm start)
NODE_ENV=development

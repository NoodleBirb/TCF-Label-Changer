{
  "name": "tcf-label-change",
  "private": true,
  "version": "0.1.0",
  "description": "HubSpot integration tool with a local web UI",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "build:bundle": "node scripts/bundle-server.mjs",
    "package": "npm run build && npm run build:bundle && node scripts/assemble-release.mjs",
    "start": "cross-env NODE_ENV=production npm run start -w server",
    "lint": "npm run lint -w client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "cross-env": "^7.0.3",
    "esbuild": "^0.25.0",
    "postject": "1.0.0-alpha.6"
  },
  "overrides": {
    "vite": "6.3.5",
    "@vitejs/plugin-react": "5.2.0"
  }
}
